# Installation

Linux & macOS
```bash
python -m venv .
source bin/activate
pip install -r requirements.txt
```

Windows (requires Bash)
```bash
python -m venv .
source Scripts/activate
pip install -r requirements.txt
```
